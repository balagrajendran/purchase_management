# --- Required ---
PORT=8080

# Set to the GCP project that hosts Firestore (Native mode)
GCP_PROJECT_ID=your-gcp-project-id

# Optional: override Firestore DB (if you created a named DB). Default is '(default)'
FIRESTORE_DB_ID=(default)

# Service Account JSON (recommended for local dev / Cloud Run without default SA)
# Paste the full JSON here (single line) OR mount as file and set GOOGLE_APPLICATION_CREDENTIALS instead.
SERVICE_ACCOUNT_JSON=

# CORS origins (comma-separated). Keep '*' for local testing.
CORS_ORIGINS=http://localhost:5173,*
